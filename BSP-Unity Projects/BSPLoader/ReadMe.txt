1. 可见多边形的生成
可视的三角形列表（即BSP中的face），通过遍历BSP树并经过视景体和背面剔除生成的，代表当前相机中潜在的可见三角形列表
2. 可见数据的组织
得到许多可见的BSP face后，接下来要做的是，将数据提交给图形引擎进行渲染，本项目中采用的方法是，将整个场景作为一个Unity
的GameObject，并为其绑定一个Mesh，所有可见的face对应Mesh中的一个Sub-Mesh